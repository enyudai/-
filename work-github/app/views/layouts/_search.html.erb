<div class="search_form">
 <%= form_with url: search_path, local: true, method: :get do |f| %>
   <%= f.text_field :word, placeholder: "Search" %>
   <%#= f.select :range, options_for_select([['Item'], ['Genre']]) %>
   <%#= f.select :search, options_for_select(["完全一致"]) %>
   <%= f.submit "Search", class: "btn btn-outline-warning" %>
 <% end %>
</div>