<!DOCTYPE html>
<html>
  <head>
    <title>PicOgi</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

<!--app/javascript 配下のファイルを参照する-->
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <!--% if user_signed_in? %>
        <div class="text-center text-white">ようこそ、<-%= current_user.nickname %>さん！</div>
      <-% elsif admin_signed_in? %>
        <div class="text-left text-white">管理者用ページ</div>
      <-% end %>-->
      
          <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <div class="container-fluid">
              <div class="collapse navbar-collapse" id="navbarSupportedContent">
                
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                  <% if user_signed_in? %>
                    <li class="nav-item">
                      <%= link_to 'マイページ', user_path(current_user.id), class: 'nav-link active' %>
                    </li>
                    
                    <li class="nav-item">
                      <%= link_to '投稿一覧', subjects_path, class: 'nav-link active' %>
                    </li>
                    
                    <li class="nav-item">
                      <%= link_to '新規投稿', new_subject_path, class: 'nav-link active' %>
                    </li>
                    
                    <li class="nav-item">
                      <%= link_to 'ホームアイコン', homes_top_path, class: 'nav-link active' %>
                    </li>
                    
                    <li class="nav-item">
                      <%= link_to 'ログアウト', destroy_user_session_path, method: :delete, class: 'nav-link active' %>
                    </li>
                    
                  <% elsif admin_signed_in? %>
                    <li class="nav-item">
                      <%= link_to 'ログアウト', destroy_admin_session_path, method: :delete, class: 'nav-link active' %>
                    </li>
                    
                  <% else %>
                    <li class="nav-item">
                      <%= link_to 'ロゴ', root_path, class: 'nav-link active' %>
                    </li>
                  <% end %>
                </ul>
                
              </div>
            </div>
          </nav>
    </header>
    
    <main>
      <%= yield %>
    </main>
  </body>
</html>
